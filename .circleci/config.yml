version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - ruby/install:
          version: '2.3.8'
      - run: echo "Ruby 2.3.8 has been installed"
      - run: ruby -v
      
orbs:
  ruby: circleci/ruby@1.1.3


workflows:
  run-jobs:
    jobs:
      - build
